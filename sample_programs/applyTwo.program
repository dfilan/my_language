main := \(x : Nat, y : Nat) {
    return applyTwo(x, y, addTwo)
}: Nat

applyTwo := \(x: Nat, y: Nat, f: Nat -> Nat -> Nat) {
    return f(x,y);
} : Nat

addTwo := \(x : Nat,y:Nat) {
    return x + y;
}: Nat